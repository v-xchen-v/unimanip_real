[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "unimanip_real"
version = "0.1.0"
description = "Real-world unmanipulated robot inference loop"
authors = [
    { name = "Xi Chen", email = "407081305@qq.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    # Add your runtime dependencies here, e.g. "numpy", "pyyaml"
]

[tool.setuptools.packages.find]
where = ["unimanip_real"]
